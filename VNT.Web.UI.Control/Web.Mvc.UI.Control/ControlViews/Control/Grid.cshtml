@*@model Web.Mvc.UI.Control.ControlModels.Grid
@(Html
    .Kendo()
    .Grid<dynamic>()
    .HtmlAttributes(new Dictionary<string, object>()
    {
        { "class", "table" }
    })
    .Name("grid")
    .DataSource(dataSource => dataSource
        .WebApi()
        .Read(action => action
            .Url(Model.WebApiUrl))))*@
@(Html.Kendo().Button().Name("aaa"))
