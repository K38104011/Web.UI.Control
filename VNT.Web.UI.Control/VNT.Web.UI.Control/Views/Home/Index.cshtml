@using VNT.Web.Mvc.UI.Control.Models
@{
    ViewBag.Title = "Home Page";
}
@{
    var container = new Container();
    var grid = new Grid
    {
        WebApiUrl = Url.Content(@"~/api/Data/GetCustomers")
    };
    container.Add(grid);
    Html.RenderContainer(container);
}
